<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.localsurveys.localsurveys">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Needed only if your app targets Android 5.0 (API level 21) or higher. -->
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:node="replace"
        android:largeHeap="true">
        <activity
            android:name=".SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/LocalSurveysTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home.HomeActivity"
            android:label="Local Surveys" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".login.SignupActivity" />
        <activity android:name=".login.ResetPasswordActivity" />
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/settings" />
        <activity
            android:name=".info.InfoActivity"
            android:label="@string/information" />
        <activity
            android:name=".createSurvey.CreateSurveyActivity"
            android:label="@string/createSurvey" />
        <activity
            android:name=".createSurvey.AddQuestionActivity"
            android:label="@string/addQuestion" />
        <activity
            android:name=".SurveyDoneActivity"
            android:label="Yay!" />
        <activity
            android:name=".survey.SurveyDetailActivity"
            android:label="@string/done" />
        <activity
            android:name=".survey.FindSurveysActivity"
            android:label="@string/done" />
        <activity
            android:name=".answerSurvey.AnswerSurveyActivity"
            android:label="Answer Survey"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <service android:name=".GPS_SERVICE"/>
    </application>

</manifest>